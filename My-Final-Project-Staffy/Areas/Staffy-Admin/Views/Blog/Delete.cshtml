@model Blog
@{
    ViewData["Title"] = "Delete";
}

<div style="color:red;">@Model.Id id-li element silinsin?</div>
<div class="mt-3">
     <div>
        <h4>
            Title:
        </h4>
        <p>
            @Model.Title
        </p>
    </div>
    <div>
        <h4>
            Subtitle:
        </h4>
        <p>
            @Model.Subtitle
        </p>
    </div>
</div>
<form method="post" asp-action="DeleteBlog" asp-route-id=@Model.Id>
    <button type="submit" class="btn btn-danger mt-4">Delete</button>
</form>

